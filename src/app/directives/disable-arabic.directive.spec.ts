import { DisableArabicDirective } from './disable-arabic.directive';

describe('DisableArabicDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableArabicDirective();
    expect(directive).toBeTruthy();
  });
});
