import { ConvrtTimePipe } from './convert-time.pipe';

describe('ConvrtTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvrtTimePipe();
    expect(pipe).toBeTruthy();
  });
});
